import XCTest
@testable import MesonMintegralAdapter

final class MesonMintegralAdapterTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MesonMintegralAdapter().text, "Hello, World!")
    }
}
